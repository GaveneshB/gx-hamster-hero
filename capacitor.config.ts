import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gx.hamsterhero',
  appName: 'GX Hamster Hero',
  webDir: 'dist-spa',
};

export default config;
